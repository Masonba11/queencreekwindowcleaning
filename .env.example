# Web3Forms — get your key at https://web3forms.com
#
# Use the PUBLIC name so the browser can submit (Web3Forms free tier does not allow
# server-side POSTs from Vercel without a paid plan + IP allowlist).
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=

# Optional: server-side only (paid Web3Forms + IP allowlist) — /api/contact
WEB3FORMS_ACCESS_KEY=
